[package]
name = "spin"
version = "0.1.0"
edition = "2021"

[build-dependencies]
custom-labels = { path = "../.." }

[dependencies]
custom-labels = { path = "../.." }
rand = "0.8.5"
tracing = "0.1.43"
tracing-subscriber = "0.3.22"
